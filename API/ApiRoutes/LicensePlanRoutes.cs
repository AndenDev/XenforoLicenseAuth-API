namespace API.ApiRoutes
{
    public static class LicensePlanRoutes
    {
        public const string Base = "api/licenseplan";
        public const string GetAll = $"{Base}/all";
        public const string GetById = $"{Base}/{{id}}";
    }
}
